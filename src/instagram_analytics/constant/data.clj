;; This namespace defines constants related to data management.
(ns instagram-analytics.constant.data
  (:require
    [clojure.core]
    ))

;; Represents the file path of the CSV file containing data.
(def csv-file-path "last_month.csv")